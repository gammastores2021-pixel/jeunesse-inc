# Public site origin used for canonical URLs and sitemaps.
NEXT_PUBLIC_SITE_URL="https://jeunesse-inc.co"

# Server-side integrations — wire when ready. Keep secrets server-only (no NEXT_PUBLIC_ prefix).
RESEND_API_KEY=""
HUBSPOT_PRIVATE_APP_TOKEN=""
ANALYTICS_HOST=""
ANALYTICS_SITE_ID=""

# Higgsfield (server-side; never expose). Used by ops scripts that pre-render hero media.
HIGGSFIELD_API_KEY=""
